import "../globals.scss";

export const metadata = {
  title: "About us at Xcursions",
  description:
    "Learn more about xcursions, the number 1 travel agent company in West Africa",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

import "../globals.scss";

export const metadata = {
  title: "Contact us at Xcursions",
  description:
    "Contact us to learn more about xcursions, the number 1 travel agent company in West Africa",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

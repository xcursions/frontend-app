export const metadata = {
  title: "Login or Signup for Xcursions",
  description: "Signup for xcursions",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
